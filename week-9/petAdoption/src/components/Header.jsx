export default function Header({ message }) {
  return (
    <header>
      <div>
        <p>PAWS & PEOPLE / ADOPTION</p>
        <h1>{message}</h1>
        <p>Tell us about your pet and the person ready to welcome them.</p>
      </div>
    </header>
  );
}
